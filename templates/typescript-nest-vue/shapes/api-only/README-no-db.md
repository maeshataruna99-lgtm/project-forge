# __PROJECT_NAME__

API-only NestJS starter generated by Project Forge. This starter has no database or persistent storage configured.

## Setup

Run `pnpm install` and `pnpm dev` to start the API at `http://localhost:3000`. Run `pnpm build` and `pnpm test` to check it.
