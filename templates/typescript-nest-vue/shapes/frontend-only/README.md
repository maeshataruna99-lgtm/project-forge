# __PROJECT_NAME__

Frontend-only Vue starter generated by Project Forge. This demo stores sample items in this browser's local storage. Demo data is local to this browser, is not backed up, and is not suitable for sensitive information. There is no API, account security, or server-side authorization in this output.
/*__OPTIONAL_README__*/

## Setup

Run `pnpm install` and `pnpm dev` to start the local demo. Run `pnpm build` to create the static site.
