# Vercel static frontend

This profile deploys only the generated Vue static site. It does not deploy a NestJS API. Keep `dataMode` set to demo and do not enable server-side authentication or authorization.
