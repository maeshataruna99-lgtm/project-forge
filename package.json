{
  "name": "project-forge",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "web:dev": "pnpm --filter @project-forge/web dev",
    "web:build": "pnpm --filter @project-forge/web build",
    "smoke:extract": "tsx scripts/smoke-generated.ts"
  },
  "devDependencies": {
    "fflate": "^0.8.2",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
